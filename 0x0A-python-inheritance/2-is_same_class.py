#!/usr/bin/python3
"""
This module checks if s object is an instance of a class
"""


def is_same_class(obj, a_class):
    """
    Returns true if is instance otherwise false
    """
    if isinstance(obj, a_class):
        return true
    else:
        return false
